# 部署与运行说明

## 安装环境

```powershell
cd D:\作业\大三下\机器学习的敏捷软件工程\期末大作业
D:\python\python.exe -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\pip.exe install -r backend\requirements.txt
```

## 推荐启动：FastAPI + LangGraph

```powershell
cd D:\作业\大三下\机器学习的敏捷软件工程\期末大作业
.\.venv\Scripts\python.exe backend\run_fastapi.py
```

浏览器访问：

```text
http://127.0.0.1:8001
```

## 兜底启动：零安装版本

```powershell
cd D:\作业\大三下\机器学习的敏捷软件工程\期末大作业
& 'C:\Users\wsnb6\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' backend\run.py
```

浏览器访问：

```text
http://127.0.0.1:8000
```

## 可选 LLM 配置

在 `.env` 中配置：

```text
OPENAI_API_KEY=your_key
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
```

不配置时系统使用规则型 Agent 兜底。

## IMAP 配置

推荐使用专用演示邮箱，并提前开启 IMAP/SMTP 授权码。

