# 测试用例与验收报告

| 用例 | 步骤 | 期望 |
|---|---|---|
| 导入样例邮件 | 点击导入样例邮件 | 邮件列表出现 5 封样例 |
| 处理任务邮件 | 选择任务邮件并运行 Agent | 生成 Todo 和草稿 |
| 处理附件邮件 | 选择附件邮件并运行 Agent | 生成附件摘要和 Todo |
| 审查模糊时间 | 处理包含“明天/本周五”的邮件 | Critic 输出确认建议 |
| Todo 状态流转 | 点击确认/完成/拒绝 | 状态更新成功 |

## 当前验收状态

已完成端到端模块验证：

- 样例邮件导入：5 封。
- Agent 处理邮件：3 封核心样例。
- Todo 生成：7 条。
- 草稿生成：2 条。
- 附件记录：1 条。
- Agent 日志：9 条。
- 本地服务健康检查：`GET /api/health` 返回成功。
- 首页访问：`GET /` 返回 200。

## FastAPI + LangGraph 升级验收

已完成 FastAPI + LangGraph 版本验证：

- 虚拟环境 `.venv` 创建成功。
- `fastapi[standard]`、`uvicorn`、`langgraph`、`langchain` 安装成功。
- `backend/app/main_fastapi.py` 导入成功。
- `backend/app/agents/langgraph_workflow.py` 导入成功。
- FastAPI 服务启动地址：`http://127.0.0.1:8001`。
- `GET /api/health` 返回：`stack = fastapi+langgraph`。
- HTTP 端到端验证通过：
  - 样例邮件导入：5 封。
  - 处理邮件：`sample-002`。
  - 工作流：`langgraph`。
  - 分类结果：`task`。
  - 风险等级：`low`。
  - Todo 生成：5 条。

## DeepSeek LLM 接入验收

已完成 DeepSeek 接入验证：

- `.env` 已读取到 `OPENAI_API_KEY`。
- `OPENAI_BASE_URL = https://api.deepseek.com/v1`。
- `OPENAI_MODEL = deepseek-chat`。
- 最小 JSON 调用返回成功：`{"ok": true, "provider": "deepseek", "message": "pong"}`。
- 重启 FastAPI 服务后，HTTP 端到端测试通过：
  - 工作流：`langgraph`。
  - 分类结果：`feature_request`。
  - Action Agent：LLM 输出。
  - 风险等级：`low`。
  - Todo 生成：1 条。
  - 草稿生成：1 条。

## 批量处理与全局 Todo 验收

已完成“同步后批量处理全部邮件”的能力验证：

- 新增接口：`POST /api/emails/process-all`。
- 验证参数：`only_unprocessed = true`，`limit = 2`。
- 请求处理邮件：2 封。
- 成功处理：2 封。
- 失败：0 封。
- 全局 Todo 汇总：12 条。
- 前端 Todo Tab 已改为展示所有邮件生成的综合 Todo，并显示来源邮件、发件人和证据。
